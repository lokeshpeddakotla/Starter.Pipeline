trigger:
- master

pool:
  vmImage: 'vs2017-win2016' # other options: 'macOS-10.13', 'vs2017-win2016'

steps:
- script: echo Hello, world!
  displayName: 'Run batchfiles'
- task: BatchScript@1
- task: BatchScript@1
  displayName: 'Run using batch script'
  inputs:
    filename: 'Install Tools/test.bat'